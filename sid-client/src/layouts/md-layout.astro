---
import Layout from "./default.astro";
import Container from "../components/container.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <Container styles="py-5 min-h-[100dvh]">
    <slot />
  </Container>
</Layout>
